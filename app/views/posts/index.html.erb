<!--<div class="nav_fit">
  <% @posts.each do |post| %>
    <div class="product">
      <%# link_to image_tag( post.image), post_path(post) %>
      <h2><%# link_to post.title, post_path(post) %></h2>
    </div>
  <% end %>

  <div style="clear:both"></div>
</div> -->

<div class="jumbotron" >
    <h1>Viewed Collection of Art</h1>
    <p>
      Please kindly sign in to add art
    </p>
    <p>
      <%# link_to "Sign up to add a review", new_user_registration_path, clase: "btn btn-primary btn-lg" %>
    </p>
</div>

<div class="row">
  <% @posts.each do |post| %>
    <div class="col-sm-6 col-md-3">
      <div class="thumbnail">
        <%= link_to (image_tag post.image.url(:medium), class: 'image'), post %>
      </div>
    </div>
  <% end %>
</div>
